# LangGraph and LangChain dependencies
langgraph
langchain
langchain-core
langchain-groq
langchain-community

python-dotenv
pydantic
typing-extensions

# Async and utility libraries
asyncio-mqtt
aiohttp
httpx
python-dateutil

# Web framework
fastapi==0.110.0
uvicorn[standard]==0.29.0

# Development and testing
groq
pytest
pytest-asyncio
pytest-mock  # For mocking in tests
pytest-cov  # For test coverage
pymongo
motor  # Async MongoDB driver (replaces MongoDB MCP + Smithery)
websockets  # For WebSocket testing

# Qdrant and RAG dependencies
qdrant-client>=1.9.1
numpy==1.26.4

# Hugging Face dependencies (unified versions)
transformers==4.57.1
huggingface-hub==0.36.0
sentencepiece==0.2.0
safetensors==0.4.5

# Redis for conversation memory cache
redis
redis[hiredis]
cachetools

# Mem0 for user memory and context management
mem0ai

# Kafka dependencies
kafka-python>=2.0.2
confluent-kafka>=2.3.0

# (Phoenix and OpenTelemetry removed; using internal no-op shims)
