roles:
  admin:
    description: "Can access all data within their business."
    permissions:
      - "data:read:all"

  developer:
    description: "Can only access data from a specific list of projects."
    # We define the exact projects the developer can see
    allowed_projects:
      - "ORRA"